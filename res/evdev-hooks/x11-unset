#!/bin/sh
set -eu

[ -n "${DISPLAY:-}" ] || exit 10
command -v xkbcomp >/dev/null 2>&1 || exit 10

runtime=${XDG_RUNTIME_DIR:-/tmp}
state_dir=$runtime/daklak-evdev-hooks
previous=$state_dir/x11-previous.xkb

[ -f "$previous" ] || exit 10
xkbcomp "$previous" "$DISPLAY"
rm -f "$previous"
